Added the following PR to the atlantis project
* --disable-apply-all
* https://github.com/runatlantis/atlantis/pull/626

This and a custom workflow script allow us to enforce a dev -> preprod -> prod workflow
